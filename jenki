pipeline {
    agent none 
    stages {
        stage('Build') {
            agent any
            steps {
                git branch: 'main', url: 'git 'https://github.com/mani017/cproject.git''
                sh '''
                    #!/bin/bash 
                    pwd 
                    ls
                    echo "This is a BUILD stage"
                    sleep 5
                '''  
            }
        }

        stage('DEPLOY') {
            agent { label 'node' } 
            steps {
                echo "This is a DEPLOY stage"
                sh 'sleep 5'
            }
        }

        stage('TESTING1') {
            agent { label 'node' } 
            steps {
                sh 'echo "This is a TESTING1 stage"'
                sh 'sleep 5'
            }
        }

        stage('TESTING2') {
            agent { label 'node' } 
            steps {
                sh '''
                    echo "This is a TESTING2 stage"
                    sleep 5
                '''
            }
        }
    }
}
